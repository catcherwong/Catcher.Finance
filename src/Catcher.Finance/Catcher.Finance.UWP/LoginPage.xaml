<Page
    x:Class="Catcher.Finance.UWP.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Catcher.Finance.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Viewbox Grid.Row="0">
        </Viewbox>
        <TextBox Grid.Row="1" x:Name="txtName" Margin="15" PlaceholderText="enter your name"  FontSize="20" Padding="12" />
        <PasswordBox Grid.Row="2" x:Name="txtPwd" Margin="15" PlaceholderText="enter your password" FontSize="20" Padding="12"  />
        <Button x:Name="btnLogin" Click="btnLogin_Click" Grid.Row="3" Margin="15" Content="Login" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
    </Grid>
</Page>
